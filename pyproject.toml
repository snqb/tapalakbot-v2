[project]
name = "tapalakbot-v2"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "httpx[http2]>=0.28",
    "pydantic>=2.0",
    "tenacity>=9.0",
]

[tool.uv.sources]
lalafo-client = { path = "packages/lalafo-client" }

[tool.uv.workspace]
members = ["packages/*"]
